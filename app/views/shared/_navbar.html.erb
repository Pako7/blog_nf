<nav class="bg-gray-800 p-4 h-16">
  <!-- Mobile Menu -->
  <% if user_signed_in? %>
    <div data-controller="menu" class="ml-3 relative">
      <div>
        <button data-action="menu#toggleDropdown" type="button" class="lg:hidden max-w-xs bg-gray-800 flex items-center text-sm rounded-lg" id="user-menu-button" aria-expanded="false" aria-haspopup="true">
          <%= svg_tag("icons/burger_menu.svg") %>
        </button>
      <div>
      <div data-menu-target="dropdown" class="hidden origin-top-left absolute left-0 mt-2 w-48 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5 focus:outline-none" role="menu" aria-orientation="vertical" aria-labelledby="user-menu-button" tabindex="-1">
        <%= link_to 'Posts', '/', role: 'menuitem', tabindex: '-1', class: "block px-4 py-2 text-sm text-gray-700 hover:text-gray-400" %>
          <%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, role: 'menuitem', tabindex: '-1', class: "block px-4 py-2 text-sm text-gray-700 hover:text-gray-400" %>
      </div>
    </div>
    <!-- Desktop Menu -->
    <div class="hidden lg:block">
      <ul class="flex space-x-4 text-white">
        <li><%= link_to 'Posts', '/', class: "hover:text-gray-400" %></li>
        <li><%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: "hover:text-gray-400" %></li>
      </ul>
    <div>
  <% end %>
</nav>


